<Window x:Class="FocusTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusTimer"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Topmost="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{x:Null}"
        AllowsTransparency="True"
        Title="MainWindow" 
        Height="{Binding Path=WindowHeight, FallbackValue=160, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
        Width="230">

    <Window.Resources>


    </Window.Resources>

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="ExitItem" Header="종료" Click="ExitItem_Click"/>
            <Separator />
            <MenuItem x:Name="InfoItem" Header="프로그램 정보" Click="InfoItem_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Border CornerRadius="6,6,6,6" Background="{StaticResource LightGray}">
        <Border BorderThickness="1" CornerRadius="6,6,6,6" Background="{Binding BackgroundColor, FallbackValue=Black}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="{Binding FixedPartLength, FallbackValue=1.4*}" />
                    <RowDefinition Height="{Binding ExpandablePartLength, FallbackValue=3*}" />
                </Grid.RowDefinitions>

                <!-- 항상 보이는 영역 -->
                <Grid Grid.Row="0" Margin="8, 0, 8, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="6*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Source="Resources/locked.png" Width="24" Style="{StaticResource BitmapImage}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    <Label Grid.Column="1" Content="{Binding ElapsedTime, FallbackValue=06:36:12}" Style="{StaticResource BigTimerLabel}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <!-- 확대되었을 때에(expanded) 보이는 영역 -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Separator Grid.Row="0" Background="{StaticResource LightGray}" Margin="0"/>

                    <Grid Grid.Row="1" Margin="0, 4, 0, 4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>

                        <local:TimerSlot Grid.Row="0" DataContext="{Binding TimerSlots[0]}" />
                        <local:TimerSlot Grid.Row="1" DataContext="{Binding TimerSlots[1]}"/>
                        <local:TimerSlot Grid.Row="2" DataContext="{Binding TimerSlots[2]}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>
