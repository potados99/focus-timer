<UserControl x:Class="FocusTimer.Charting.UsageByTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FocusTimer.Charting"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:UsageByTimeItem}"
             d:DesignHeight="30" d:DesignWidth="120">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="40" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Style="{StaticResource WhiteTextLabel}" Content="{Binding TimeString, FallbackValue=언제언제}" HorizontalAlignment="Left" VerticalAlignment="Center" />
        <Label Grid.Column="1" Style="{StaticResource WhiteTextLabel}" Content="{Binding UsageMinutesString, FallbackValue=10분}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
